name: Invalid module type
priority: BLOCKER
tags: compile-time,runtime-error
type: BUG
description:
The module that has been referenced here has an invalid type.

This is raised e.g. when `PARAMETER USING` a LDA etc.
