name: COMPRESS can't have LEAVING NO and WITH DELIMITERS
priority: BLOCKER
tags: compile-time
type: BUG
description:
A ``COMPRESS`` statement can not specify both ``LEAVING NO`` and ``WITH DELIMITERS``.

``WITH DELIMITERS`` already implies ``LEAVING NO``.
